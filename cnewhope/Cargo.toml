[package]
name = "cnewhope"
version = "0.1.0"
authors = ["quininer kel <quininer@live.com>"]
build = "build.rs"

[lib]
path = "lib.rs"

[build-dependencies]
gcc = "0.3"

[dev-dependencies]
newhope = { path = "..", version = "0.1" }

[features]
tor = [ "newhope/tor" ]
